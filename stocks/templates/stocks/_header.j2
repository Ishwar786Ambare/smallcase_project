<!-- Main Header -->
<header class="main-header">
    <div class="header-container">
        <a href="{{ url('home') if request.user.is_authenticated else url('login') }}" class="logo">
            <span class="logo-icon">📊</span>
            <span>StockBasket</span>
        </a>

        {% if request.user.is_authenticated %}
        <nav>
            <ul class="nav-menu">
                <li><a href="{{ url('home') }}">🏠 Dashboard</a></li>
                <li><a href="{{ url('basket_create') }}">➕ Create Basket</a></li>
                <li><a href="{{ url('populate_stocks') }}">📥 Load Stocks</a></li>
                <li><a href="{{ url('update_prices') }}">🔄 Update Prices</a></li>
            </ul>
        </nav>

        <div class="user-info">
            <div>
                <div class="user-name">👤 {{ request.user.username }}</div>
                <div class="user-email">{{ request.user.email }}</div>
            </div>
            {% include 'stocks/_theme_toggle.j2' %}
            <a href="{{ url('logout') }}" class="btn-logout">Logout</a>
        </div>
        {% else %}
        <div style="display: flex; gap: 15px; align-items: center;">
            {% include 'stocks/_theme_toggle.j2' %}
            <a href="{{ url('login') }}" class="btn-auth">Login</a>
            <a href="{{ url('signup') }}" class="btn-auth">Sign Up</a>
        </div>
        {% endif %}
    </div>
</header>
